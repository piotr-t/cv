import { AsideIconsDirective } from './aside-icons.directive';

describe('AsideIconsDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideIconsDirective();
    expect(directive).toBeTruthy();
  });
});
